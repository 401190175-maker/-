# 图块图谱方案

## 1. 文档目的

本文定义图块知识图谱的最终目标架构，以及从当前图谱演进到“按需多模态识别、逐步缓存、确定性规则自动确认、多模态大模型独立复核候选关系”的方案。

本方案只讨论知识图谱规划，不包含代码实现、模型选型、HTTP/REST API、Agent Skill 实现或部署方案。

## 2. 已确认的设计决定

本方案基于以下已经确认的决定：

1. 图块图谱采用三层架构：
   - 来源事实层；
   - 空间与上下文派生关系层；
   - 语义证据层。
2. 页面级来源关系统一归入来源事实层。
3. block 级派生关系统一归入空间与上下文派生关系层。为便于表述，后文简称“空间关系层”。
4. 多模态模型对图纸局部或整体区域产生的文字观察、结构化解析、候选匹配、确认状态和推理来源统一归入语义证据层；`CrossSection` 与 `BlockCaption` 的断面匹配只是其中一种应用场景。
5. 采用“按需识别并逐步缓存”的方案，不在当前阶段强制执行全量离线 OCR。
6. 可以预留 `MATCHES_SECTION_CAPTION` 关系的语义、方向和输出接口；唯一、满足确定性规则且不存在冲突的匹配可以自动生成正式关系，其他情况只生成 `CANDIDATE_MATCHES_SECTION_CAPTION` 候选关系。
7. 分开表达物理剖面区域和逻辑断面标签；一个 `CrossSection` 标注区域始终对应一个节点，不因区域内出现两个相同标签而拆分。
8. `CrossSection` 与 `BlockCaption` 的候选匹配范围限定在同一 `DrawingPage`。
9. 查询结果采用统一的证据输出格式。
10. 图块解析采用“结构化字段 + 自然语言总结”，作为语义证据层中的独立 `BlockInterpretation`，不直接写入 `DrawingBlock` 来源事实。
11. `DrawingBasicInfo` 只与 `DrawingPage` 建立来源或上下文关系，不再向页面内每个 `DrawingBlock` 批量创建关系；图块查询通过所属页面取得基础信息。
12. 返回图块或页面元素时，稳定业务 ID 只作为证据引用；同时返回按需识别并缓存的文字、表格结构、图块结构或自然语言解析。
13. 断面标签采用“符号体系 + 规范值”的通用模型，支持字母、罗马数字、阿拉伯数字和字母数字组合；`Ⅰ/Ⅱ/Ⅲ` 只是示例，不是支持范围。
14. 不默认合并外观相近但符号体系不同的标签，例如 `I-I`、`Ⅰ-Ⅰ` 和 `1-1`；只有可审计的明确图纸规则、独立多模态复核或人工最终确认才能建立别名映射。
15. `RecognitionRun` 不作为知识图谱节点，而是图谱外部的识别运行日志；证据节点通过 `recognition_run_id` 回查模型、提示词、时间、状态和错误。
16. `SectionLabelAliasRule` 不作为知识图谱节点，而是图谱外部的版本化规则配置；使用结果在候选边或正式边上记录 `alias_rule_id` 和 `alias_rule_version`。
17. 不建立 `RelationAssessment` 节点。未确认的具体判断使用带属性的 `CANDIDATE_*` 候选关系，已成立事实使用正式关系。
18. 简单、唯一且没有规则冲突的关系可以由确定性规则自动确认；其余 `CANDIDATE_*` 候选关系优先交由多模态大模型独立复核，人工只作为模型仍无法裁决且业务确有需要时的最终兜底。
19. 候选复核必须是独立的第二次判断：模型应同时看到全部候选、原始裁剪、页面上下文、空间证据和已有观察，不能只读取第一次识别生成的规范化文字进行自我确认。

## 3. 总体目标

最终图块图谱应同时具备以下能力：

- 定位：能够找到项目、图纸册、页面、图块和页面元素。
- 追溯：任何查询结果都能回到原始图片、页面和 bbox。
- 关联：能够表达图块与标题、基础信息、注释、剖面标记之间的派生关系。
- 识别：能够按需读取 `CrossSection` 和 `BlockCaption` 中的文字。
- 补充：能够为存在图块的图纸页取得本页或所属图纸组的 `DrawingBasicInfo`，并返回其识别内容。
- 匹配：能够将物理剖面标记映射为带符号类型的逻辑断面标签，再寻找对应的断面标题和目标图块。
- 解析：能够按需识别图块的构件、内部文字、尺寸、材料、构造特征和空间关系，并提供面向用户的自然语言总结。
- 表达：标题、基础信息、表格、注释和普通文字均返回可读语义内容，而不只返回节点 ID。
- 审计：能够通过证据上的 `recognition_run_id` 说明某个识别或匹配结果来自哪次模型调用、哪个裁剪区域和哪个版本。
- 保守：证据不足、结果冲突或候选不唯一时，不把推测写成正式图谱事实。

最终图谱不是把所有模型回答都写进 Neo4j，而是将稳定来源事实、可复核派生关系、可追溯语义证据和经过确认的语义关系分开管理。

## 4. 三层架构

```text
来源事实层
  -> 空间与上下文派生关系层
  -> 语义证据层
  -> 统一查询输出
```

配套架构图见：[图块图谱目标三层架构.svg](./图块图谱目标三层架构.svg)。图中实线表示当前已有关系，虚线表示目标规划关系，点划线表示 `CANDIDATE_*` 候选关系；运行日志和规则配置明确位于核心图谱之外。

### 4.1 来源事实层

来源事实层保存原始标注和基础导入能够直接确定的事实。

核心层级：

```text
Project
  -> DrawingSet
    -> DrawingPage
      -> DrawingBlock / 页面元素
```

主要节点：

- `Project`
- `DrawingSet`
- `DrawingPage`
- `DrawingBlock`
- `Table`
- `TableCaption`
- `BlockCaption`
- `CrossSection`
- `DrawingBasicInfo`
- `DrawingAnnotation`
- `PlainText`
- `Title`
- `IgnoredElement`
- `ImportBatch`

主要关系：

- `Project -[:HAS_SET]-> DrawingSet`
- `DrawingSet -[:HAS_PAGE]-> DrawingPage`
- `DrawingPage -[:HAS_BLOCK]-> DrawingBlock`
- `DrawingPage -[:HAS_TABLE]-> Table`
- `DrawingPage -[:HAS_BASIC_INFO]-> DrawingBasicInfo`
- `DrawingPage -[:HAS_ANNOTATION]-> DrawingAnnotation`
- `DrawingPage -[:HAS_TEXT]-> PlainText`
- `DrawingPage -[:HAS_TEXT]-> Title`
- `DrawingPage -[:HAS_ELEMENT]-> BlockCaption`
- `DrawingPage -[:HAS_ELEMENT]-> TableCaption`
- `DrawingPage -[:HAS_ELEMENT]-> CrossSection`
- `DrawingPage -[:HAS_ELEMENT]-> IgnoredElement`
- `DrawingPage -[:IMPORTED_IN]-> ImportBatch`

这一层回答“数据来自哪里”和“元素位于哪里”，不依赖多模态模型推理。

### 4.2 空间与上下文派生关系层

这一层保存通过 bbox、页面范围、页码顺序或上下文规则得到的 block 级派生关系。

主要关系：

- `DrawingBlock -[:HAS_CAPTION]-> BlockCaption`
- `DrawingBlock -[:HAS_ANNOTATION]-> DrawingAnnotation`
- `DrawingBlock -[:HAS_SECTION_MARK]-> CrossSection`
- `Table -[:HAS_CAPTION]-> TableCaption`
- `DrawingPage -[:USES_BASIC_INFO]-> DrawingBasicInfo`

复杂、冲突或多候选的空间判断使用候选关系：

- `BlockCaption -[:CANDIDATE_CAPTION_OF]-> DrawingBlock`
- `DrawingBlock -[:CANDIDATE_HAS_SECTION_MARK]-> CrossSection`

其中：

- `HAS_CAPTION` 主要来自同页空间匹配。
- `HAS_SECTION_MARK` 主要来自 bbox 包含或显著重叠。
- `CANDIDATE_CAPTION_OF` 表示标题与图块之间存在归属可能，但当前无法唯一确定。
- `CANDIDATE_HAS_SECTION_MARK` 表示剖面标记与图块之间存在包含或重叠候选，但当前空间规则无法唯一确定。
- `Table -[:HAS_CAPTION]-> TableCaption` 与 block 标题关系一样，属于同页空间派生关系，不归入来源事实层。
- `HAS_ANNOTATION` 表达同页共享上下文，不是纯粹的几何关系。
- `HAS_BASIC_INFO` 是 `DrawingPage` 与本页实际标注基础信息之间的来源事实关系。
- `USES_BASIC_INFO` 是页面级上下文关系，表示某个存在图块的图纸页使用哪一份本页或图纸组基础信息；不向该页每个图块复制关系。

因此本层正式名称为“空间与上下文派生关系层”。简称“空间关系层”时，不应将所有关系误解为单纯的距离或重叠关系。

`DrawingBasicInfo` 的目标机制如下：

1. 页面没有 `DrawingBlock` 时，不要求补充或继承 `DrawingBasicInfo`。如果该页本身确有 `DrawingBasicInfo` 标注，仍保留其来源事实，但不据此认定该页一定是图纸页。
2. 页面存在 `DrawingBlock` 时，将其视为待判断的图纸页，首先查找本页实际标注的 `DrawingBasicInfo`。
3. 本页存在基础信息时，以本页信息为优先上下文。
4. 本页不存在基础信息时，才在同一候选图纸组内寻找与本页页码最接近的存在DrawingBasicInfo信息的前后两张图纸页上的基础信息锚点。
5. 图纸组边界优先依据已解析基础信息中的项目、专业、图名、图号等公共字段判断；连续页码只用于缩小候选范围，不能单独证明属于同一组。
6. 第4条找到的锚点公共字段一致时，可为组内缺失基础信息的 block 页面建立页面级 `USES_BASIC_INFO`。
7. 只有一个可靠锚点时可以返回 `partial`；首尾锚点冲突或组边界不唯一时返回 `ambiguous`，不得强行继承。
8. `USES_BASIC_INFO` 建议记录 `source`、`source_page_id`、`group_id`、`rule_version`、`status` 和证据引用。`source` 可取 `current_page`、`group_start` 或 `group_end`。

空间候选边建议统一记录 `status`、`candidate_count`、`score`、`conflict_reason`、`rule_version`、证据引用和时间。候选唯一、满足空间阈值且没有规则冲突时，系统可以直接建立 `HAS_CAPTION` 或 `HAS_SECTION_MARK`；否则保留对应 `CANDIDATE_*` 边并优先由多模态大模型复核，不要求用户确认每一个简单关系。

图块查询基础信息时采用以下路径：

```text
DrawingBlock
  <-[:HAS_BLOCK]-
DrawingPage
  -[:HAS_BASIC_INFO|USES_BASIC_INFO]->
DrawingBasicInfo
  -[:HAS_INTERPRETATION]->
BasicInfoInterpretation
```

因此，一个页面有多少个图块都不会导致基础信息关系按图块数量膨胀。

这一层回答：

- 某个图块的标题区域在哪里？
- 某个图块所属页面使用哪些基础信息，以及这些信息来自本页还是图纸组锚点？
- 某个图块共享哪些页面注释？
- 某个物理剖面标记落在哪个来源图块中？

### 4.3 语义证据层

语义证据层保存多模态模型对图纸局部区域或整体区域产生的可缓存、可追溯观察与解释，以及尚未成为正式事实的具体候选语义边。

这一层包括文字观察、断面标签规范化结果、结构化解析、候选匹配、确认状态和推理来源。它不只服务 `CrossSection` 与 `BlockCaption` 的剖面匹配，也服务 `DrawingBlock`、`Title`、`PlainText`、`DrawingBasicInfo`、`Table`、`TableCaption` 和 `DrawingAnnotation` 等图纸对象的按需识别与缓存。

这一层不把模型输出直接视为原始图纸事实，而是将其表达为“模型对某个图像区域做出的观察或解释”。来源事实仍由原始 JSON、图片、页面、稳定业务 ID 和 bbox 承担。

核心图谱中的概念数据包括 `TextObservation`、各类 `Interpretation`、候选关系和正式语义关系。识别运行日志与匹配规则配置位于核心图谱之外。

#### 图谱外部识别运行日志

`RecognitionRun` 是运行日志概念，不是知识图谱节点。它服务模型调用监控、错误诊断、成本统计和证据回查，不参与图纸对象之间的知识关系。

每次真实调用多模态模型时，外部日志至少记录：

- `run_id`；
- `run_type`：`recognition`、`interpretation` 或 `candidate_review`；
- 查询或任务范围；
- 模型名称和版本；
- 提示词版本；
- 运行时间和耗时；
- 输入图片及其内容哈希；
- 成功、部分成功或失败状态；
- 错误与警告摘要。

缓存命中和普通用户查询不创建新的运行日志。`TextObservation` 与各类 `Interpretation` 保存 `recognition_run_id`；候选边和正式边保存独立的 `review_run_id`。两类 ID 都回查同一个图谱外运行日志模块，只是 `run_type` 不同，不建立运行节点。

#### `TextObservation`

表示模型对一个具体裁剪区域中可读文字、符号、编号、尺寸或标注片段的观察。

`TextObservation` 是通用文字观察节点，不是 `CrossSection` 专用节点。图块内部的尺寸文字、材料标注、构件编号、局部说明和其他可读文字也使用 `TextObservation` 保存；不再为图块内部文字另建新的文字节点类型。

建议记录：

- 观察 ID；
- `recognition_run_id`，用于回查图谱外部运行日志；
- 目标元素 ID；
- 目标元素类型；
- 原始识别文字；对于 `BlockCaption`，保存模型实际读取到的连续标题文字；对于 `DrawingBlock` 内部文字，保存模型实际读取到的局部文字或符号片段；
- 规范化文字；
- 规范化规则版本；
- 语义专项字段；例如 `CrossSection` 的端点标签、符号体系和逻辑键，见 4.4；
- 图片路径；
- 裁剪 bbox；
- 模型给出的置信描述；
- 是否存在字符歧义；
- 创建时间；
- 缓存键。

`TextObservation` 可以绑定到：

- `CrossSection`
- `BlockCaption`
- `TableCaption`
- `Title`
- `PlainText`
- `DrawingBasicInfo`
- `DrawingAnnotation`
- `DrawingBlock` 内部局部文字区域

来源元素与文字观察之间使用显性关系：

```text
来源元素 -[:HAS_OBSERVATION]-> TextObservation
```

该关系回答“这条文字观察描述哪个图纸对象或局部区域”；`recognition_run_id` 回答“哪次模型调用产生了它”。两者职责不同，不构成功能重叠。

`TextObservation` 表达的是“模型观察到了什么”，不是“图纸上的真实文字已经被证明是什么”。

#### `BlockInterpretation`

表示多模态模型对一个完整 `DrawingBlock` 裁剪区域生成的一次图块解析。

关系形式：

```text
DrawingBlock -[:HAS_INTERPRETATION]-> BlockInterpretation
```

建议记录：

- 解析 ID；
- `recognition_run_id`，用于回查图谱外部运行日志；
- `DrawingBlock` 稳定业务 ID；
- `summary`：面向用户的自然语言总结；
- `interpreted_type`：AI 判断的平面图、剖面图、节点详图或其他类型；
- `components`：识别到的梁、柱、墙、板、基础、钢筋或其他构件关键词；
- `internal_annotation_refs`：图块内部文字观察的稳定引用；
- `construction_features`：连接方式、构造层次、配筋形式或布置特征；
- `spatial_relations`：构件之间的上下、内外、连接、支承或相邻关系；
- `materials`：识别到的材料、强度等级、型号或规格；
- `dimensions`：识别到的尺寸、标高、角度或数量；
- `uncertainties`：看不清、相互冲突或无法确认的内容；
- `analysis_status`：`interpreted`、`partial`、`ambiguous`、`failed` 或 `stale`；
- `payload_ref`：完整不可变结构化解析结果的引用；
- 模型名称和版本；
- 提示词版本；
- 解析数据契约版本；
- 图像内容哈希；
- 图块 bbox；
- 创建时间；
- 缓存键。

`interpreted_type` 只属于 AI 解析结果，不得写回或替代 `DrawingBlock.block_type`。同一图块允许保留多个不同模型、提示词或数据契约版本的解析，旧结果不得被新结果静默覆盖。

`BlockInterpretation` 表达完整图块的综合解析，例如类型、构件、材料、尺寸、构造特征、空间关系和自然语言总结。图块内部可读文字不直接作为不可追溯文本塞进 `BlockInterpretation`，而是生成或复用对应的 `TextObservation`，再由 `internal_annotation_refs` 或 `payload_ref` 引用。

当图块解析使用了文字观察时，建立显性证据关系：

```text
BlockInterpretation -[:SUPPORTED_BY]-> TextObservation
```

同一机制也适用于其他 Interpretation；它表示“解析依据了哪些文字证据”，不替代来源元素的 `HAS_INTERPRETATION` 或 `HAS_OBSERVATION`。

第一版存储边界：

- `BlockInterpretation` 节点保存自然语言总结、状态、版本信息和便于检索的关键词列表。
- 图块内部文字继续使用 `TextObservation` 保存，并保留每段文字的局部 bbox。
- 构件、文字、尺寸、材料和空间关系的完整嵌套结构保存为不可变 JSON 产物，由 `payload_ref` 引用。
- 查询服务组合 Neo4j 中的图块证据、`BlockInterpretation` 和完整 JSON 产物后统一返回。
- 缓存命中只表示可复用既有 AI 解析，不表示解析内容已经成为来源事实。

#### `BasicInfoInterpretation`

表示多模态模型对一个 `DrawingBasicInfo` 区域生成的结构化基础信息解析。

关系形式：

```text
DrawingBasicInfo -[:HAS_INTERPRETATION]-> BasicInfoInterpretation
```

建议记录：

- 解析 ID、`recognition_run_id` 和 `DrawingBasicInfo` 稳定业务 ID；
- 原始全文和面向用户的自然语言总结；
- 项目名称、图名、专业、图号、比例、设计、审核、日期等可识别字段；
- 材料说明、通用说明或其他补充字段；
- 字段级原始文字、置信描述和证据 bbox；
- `analysis_status`、`uncertainties` 和冲突字段；
- `payload_ref`、模型版本、提示词版本、数据契约版本、图像哈希和缓存键。

`BasicInfoInterpretation` 既用于用户回答，也用于判断图纸组首尾锚点是否一致。模型解析结果不能覆盖 `DrawingBasicInfo` 的来源 bbox、页面归属或稳定业务 ID。

使用文字观察作为字段证据时，可建立：

```text
BasicInfoInterpretation -[:SUPPORTED_BY]-> TextObservation
```

#### `TableInterpretation`

表示多模态模型对一个 `Table` 区域生成的表格结构解析。

关系形式：

```text
Table -[:HAS_INTERPRETATION]-> TableInterpretation
```

建议记录：

- 解析 ID、`recognition_run_id` 和 `Table` 稳定业务 ID；
- 表格标题或关联 `TableCaption` 的引用；
- 表头、行、列、单元格内容和合并单元格信息；
- 面向用户的表格摘要；
- 无法确定的行列边界、合并关系和文字内容；
- `analysis_status`、`uncertainties`、`payload_ref`；
- 模型、提示词、数据契约、图片哈希和缓存版本。

使用文字观察作为单元格或表头证据时，可建立：

```text
TableInterpretation -[:SUPPORTED_BY]-> TextObservation
```

标题、注释和普通文字以 `TextObservation` 返回原文与规范化文本；基础信息、表格和图块分别使用专门的 Interpretation 表达结构化语义。稳定 ID、原图路径和 bbox 始终与语义内容一同返回。

### 4.4 CrossSection 语义匹配专项

本节只说明 `CrossSection` 如何使用语义证据层中的通用概念。它不是新的顶层架构层，也不改变 `TextObservation`、`BlockInterpretation`、图谱外部识别日志或版本化别名规则配置的概念边界。

`CrossSection` 的来源事实仍然是一个物理标注区域：节点来自原始标注 bbox，表示图纸上一个完整剖面标记。语义证据层只负责记录模型在这个区域内观察到的端点文字、符号体系、规范化逻辑键、候选标题和确认状态。

#### `CrossSection` 的 `TextObservation`

当 `TextObservation` 绑定到 `CrossSection` 时，除通用文字观察字段外，还需要记录剖面匹配所需的专项字段：

- `marker_components`：端点标签分量；分别记录每个端点的原始标签、局部 bbox 和相对位置；
- `pair_orientation`：两个端点的排列方向，取值为 `vertical`、`horizontal` 或 `unknown`；
- `component_count`：端点数量；
- `label_system`：断面标签符号体系；
- `canonical_label`：断面标签规范值；
- `normalized_section_key`：由符号体系和规范值共同生成的逻辑键；
- `normalization_rule_version`：断面标签规范化规则版本；
- `has_character_ambiguity`：是否存在字符或符号体系歧义。

这些字段只表达模型对剖面标记区域的观察和规范化结果，不改变 `CrossSection` 节点数量，也不把模型输出写回来源事实。

#### `BlockCaption` 的断面标题观察

当 `TextObservation` 绑定到 `BlockCaption` 且用于断面匹配时，需要保留完整标题原文，并从标题中提取成对核心标签。例如 `A-A剖面`、`Ⅰ-Ⅰ断面图` 或 `1-1` 可以进一步生成：

- 标题原文；
- 成对核心标签；
- `label_system`；
- `canonical_label`；
- `normalized_section_key`；
- `normalization_rule_version`；
- 字符或符号体系歧义。

`CrossSection` 与 `BlockCaption` 的原始视觉形式不同：前者可能是上下两个端点标签，后者通常是连续标题文字。二者只有在生成可比较的 `normalized_section_key` 后，才可以进入候选匹配。

#### 图谱外部版本化别名规则配置

`SectionLabelAliasRule` 表示某个图纸范围内，被确认可以互相等价使用的断面标签规则，但它不是图纸对象，也不是模型直接观察到的证据，因此不作为核心知识图谱节点。

例如，模型可能把外观相近的断面标题识别为 `I-I`、`Ⅰ-Ⅰ` 或 `1-1`。系统默认不认为它们相同，避免错误匹配。只有当明确图纸说明、独立多模态复核或人工最终审核确认它们在当前页面、图纸组或图纸册中表示同一个断面时，才记录一条别名规则。

建议存储在独立规则配置表或版本化配置文件中，并记录：

- `alias_rule_id`；
- `left_section_key` 与 `right_section_key`；
- 适用范围类型和范围 ID，例如 `DrawingPage`、图纸组或 `DrawingSet`；
- 图纸说明、观察或人工确认等依据；
- `status`：`candidate`、`confirmed`、`ambiguous`、`revoked`；
- `confirmation_method`：`explicit_drawing_rule`、`multimodal_llm` 或 `human`；
- 确认主体、确认时间和规则版本；
- 创建该规则所依据的 `recognition_run_id`、证据引用或人工审核记录。

`status` 的含义和处理方式如下：

| 状态 | 含义 | 处理方式 |
|---|---|---|
| `candidate` | 可能等价，但尚未确认 | 优先进入独立多模态复核；不参与确定匹配；模型仍无法裁决且业务需要时才人工审核 |
| `confirmed` | 已确认在指定范围内等价 | 只有查询对象落在适用范围内时，才能参与匹配 |
| `ambiguous` | 存在多个合理解释、证据冲突或适用范围不唯一 | 返回歧义和候选证据；不参与确定匹配；不写入正式关系 |
| `revoked` | 规则已撤销或被证明不可靠 | 保留审计记录；不再参与匹配；依赖它的匹配缓存应失效 |

只有 `confirmed` 且查询对象落在适用范围内的规则才能参与匹配。具有明确图纸说明和唯一作用域时，独立多模态复核也可以将图谱外部别名配置确认为 `confirmed`；仅凭字符外观相近不得确认。别名规则不修改原始 `TextObservation.normalized_section_key`，只在查询和匹配时作为额外证据使用。依赖别名规则的候选边或正式边保存 `alias_rule_id` 与 `alias_rule_version`，不在图中连接到规则节点。

### 4.5 三层之间的边界

三层必须保持以下边界：

- 原始标注节点和页面来源关系不因模型结果而被覆盖。
- block 级几何关系不因语义识别结果而被删除。
- 模型观察必须带有模型版本、裁剪区域和 `recognition_run_id`，并能回查图谱外部运行日志。
- `BlockInterpretation` 必须与 `DrawingBlock` 来源事实分离，AI 推断类型、构件和构造不得覆盖原始节点属性。
- `BasicInfoInterpretation` 和 `TableInterpretation` 必须与来源元素分离，不得覆盖来源 bbox 或页面归属。
- `DrawingBasicInfo` 上下文只在页面级建立；不创建 `DrawingBlock -[:HAS_BASIC_INFO]-> DrawingBasicInfo` 的批量关系。
- `SectionLabelAliasRule` 只能作为图谱外部、有范围、有证据和有确认状态的版本化规则配置存在，不得改写原始文字观察。
- 不同版本的图块解析必须可并存、可审计，并能根据缓存规则判定有效或失效。
- 未确认的具体匹配使用带状态和证据属性的 `CANDIDATE_*` 关系，不建立 `RelationAssessment` 节点。
- 唯一、满足确定性规则且无冲突的语义匹配可以自动写入正式关系；复杂、冲突或多候选情况必须先保留候选边，再由独立多模态复核裁决；模型仍无法唯一判断时保持 `ambiguous`，必要时才人工确认。

### 4.6 存储形式总表

| 内容 | 推荐存储形式 |
|---|---|
| 图纸对象 | 核心知识图谱节点 |
| 页面级来源事实 | 来源事实层正式关系 |
| 已经唯一成立的空间或语义事实 | 正式关系边 |
| 未确认的具体匹配 | `CANDIDATE_*` 候选关系边 |
| 判断依据、状态、分数和冲突原因 | 候选边或正式边的属性 |
| 模型文字观察 | `TextObservation` 证据节点 |
| 模型结构化解析 | `BlockInterpretation`、`BasicInfoInterpretation`、`TableInterpretation` 证据节点 |
| 识别运行过程 | 图谱外部运行日志，通过 `recognition_run_id` 回查 |
| 编号别名规则 | 图谱外部版本化规则配置，通过 `alias_rule_id` 和版本引用 |
| 缓存键 | 证据节点或匹配缓存记录的属性，不建立独立节点 |

核心原则是：图纸对象作为节点，正式事实作为边，未确认判断作为候选边，模型输出作为证据节点，算法运行和配置规则留在图谱之外。

## 5. 物理标记与逻辑断面标签

### 5.1 物理标记

一个 `CrossSection` 节点表示图纸上一个被标注出来的完整物理区域，节点粒度以 XAnyLabeling 中的一个 `CrossSection` 标注 bbox 为准，具有独立的：

- 稳定业务 ID；
- 所属页面；
- bbox；
- 原图引用。

一个 `CrossSection` 标注区域中通常不是连续书写完整的 `A-A` 或 `Ⅰ-Ⅰ`，而是在矩形框的上下两端或左右两端各出现一个相同标签，例如 `A`、`A` 或 `Ⅰ`、`Ⅰ`。两个端点标签及其之间的剖切线共同构成一个完整剖面标记区域，始终归属于一个 `CrossSection` 节点，不能按照端点数量拆成两个节点。

只有当原始标注中确实存在两个独立的 `CrossSection` bbox 时，才创建两个独立节点。节点数量由标注区域数量决定，不由区域内识别出的字符数量决定。

### 5.2 通用逻辑断面标签

逻辑断面标签是识别和规范化之后得到的业务标识，由以下三个部分组成：

- `label_system`：符号体系；
- `canonical_label`：该符号体系内的规范值；
- `normalized_section_key`：由符号体系和规范值共同生成的逻辑键。

第一版支持以下符号体系：

| `label_system` | 典型原始形式 | 规范值示例 | 逻辑键示例 |
|---|---|---|---|
| `alphabetic` | `A`、`a`、`B`、`C` | `A`、`B`、`C` | `SECTION_ALPHA_A` |
| `roman` | `Ⅰ`、`Ⅱ`、`Ⅲ` | `1`、`2`、`3` | `SECTION_ROMAN_1` |
| `numeric` | `1`、`01`、`2` | `1`、`01`、`2` | `SECTION_NUMERIC_1`、`SECTION_NUMERIC_01` |
| `alphanumeric` | `A1`、`B2`、`1A` | `A1`、`B2`、`1A` | `SECTION_ALPHANUMERIC_A1` |
| `unknown` | 无法可靠判断的字符 | 不生成 | `unknown` |

`CrossSection` 与 `BlockCaption` 的原始视觉形式不同，但可以在相同符号体系内映射到同一个逻辑键：

| 来源 | 原始识别形式 | 规范化逻辑键 |
|---|---|---|
| `CrossSection` | 两端分别为 `A`、`A` | `SECTION_ALPHA_A` |
| `BlockCaption` | `A-A`、`a—a`、`A－A剖面` | `SECTION_ALPHA_A` |
| `CrossSection` | 两端分别为 `B`、`B` | `SECTION_ALPHA_B` |
| `BlockCaption` | `B-B`、`B—B断面图` | `SECTION_ALPHA_B` |
| `CrossSection` | 两端分别为 `Ⅰ`、`Ⅰ` | `SECTION_ROMAN_1` |
| `BlockCaption` | `Ⅰ-Ⅰ`、`Ⅰ—Ⅰ剖面` | `SECTION_ROMAN_1` |
| `CrossSection` | 两端分别为 `1`、`1` | `SECTION_NUMERIC_1` |
| `BlockCaption` | `1-1`、`1—1断面` | `SECTION_NUMERIC_1` |
| `CrossSection` | 两端分别为 `A1`、`A1` | `SECTION_ALPHANUMERIC_A1` |
| `BlockCaption` | `A1-A1`、`A1—A1剖面` | `SECTION_ALPHANUMERIC_A1` |

上述表格只是示例，不是枚举白名单。字母体系可继续支持 `D-D` 至 `Z-Z`，数字体系可支持任意合理整数编号，字母数字体系可支持符合数据契约的组合标签。

规范化必须满足：

- 保留原始识别文字，不覆盖原文。
- `CrossSection` 分别保留两个端点标签及其局部 bbox、相对位置和排列方向，不虚构中间横线。
- `BlockCaption` 保留完整标题原文，并从“`A-A剖面`”或“`A-A断面图`”等上下文中提取成对核心标签。
- 统一全角、半角、横线类型、大小写和无语义空格；字母默认规范为大写。
- 在同一符号体系内规范化表现差异，例如 `a—a` 与 `A-A`。
- 数字标签默认保留有意义的前导零，因此 `01-01` 与 `1-1` 默认生成不同逻辑键；只有已确认别名规则才能合并。
- 成对标签的左右部分或两个物理端点必须在规范化后相同；例如 `A-B` 不能生成一个确定逻辑键。
- 符号体系必须参与逻辑键生成，不能只保存一个不带类型的值。
- `I-I` 默认按拉丁字母处理为 `SECTION_ALPHA_I`；`Ⅰ-Ⅰ` 为 `SECTION_ROMAN_1`；`1-1` 为 `SECTION_NUMERIC_1`。三者不得仅凭外观相近自动合并。
- 无法可靠判断字符或符号体系时返回 `unknown`，不强行猜测。

### 5.3 一个物理区域内的成对同名标签

当一个 `CrossSection` 标注区域的上下两端或左右两端各出现一个相同标签时：

- 图谱只保留一个 `CrossSection` 节点。
- 该节点产生一个 `TextObservation`。
- 观察分别记录两个标签分量及其端点位置和局部 bbox。
- `pair_orientation` 记录两个端点是上下排列还是左右排列。
- `component_count` 记录为 `2`，但不能据此拆分节点。
- 不得为了便于匹配而把原始观察伪造成图中不存在的连续标题文字。
- 两端 `A`、`A` 规范化为 `SECTION_ALPHA_A`，标题 `A-A剖面` 也规范化为 `SECTION_ALPHA_A`。
- 两端 `Ⅰ`、`Ⅰ` 规范化为 `SECTION_ROMAN_1`，标题 `Ⅰ-Ⅰ` 也规范化为 `SECTION_ROMAN_1`。
- 该 `CrossSection` 节点可以与同页、逻辑键相同的候选 `BlockCaption` 进行语义匹配。

如果两个端点只识别出一个、两个端点标签不一致、符号体系无法判断，或者同页存在多个同键标题，系统必须降低结论等级并返回歧义信息，但仍保持一个 `CrossSection` 节点。

### 5.4 跨符号体系别名

少数图纸可能混用 `I-I`、`Ⅰ-Ⅰ` 和 `1-1` 表达同一断面，但这种等价性不能作为全局默认规则。

允许在以下条件下使用跨体系别名：

1. 别名规则限定到明确的 `DrawingPage`、图纸组或 `DrawingSet`，不能无边界全局生效。
2. 规则来自图纸中的明确说明、同页唯一对应证据或人工确认。
3. 规则记录 `alias_rule_id`、适用范围、创建依据、确认方式、版本和时间。
4. 查询输出同时保留原始符号体系、原始逻辑键和应用别名后的匹配键。
5. 别名规则未确认、存在冲突或适用范围不唯一时，返回 `ambiguous`，不得建立正式匹配关系。

别名规则保存在图谱外部。使用它得到的候选边或正式边只记录 `alias_rule_id` 和 `alias_rule_version`；规则内容、适用范围和审核记录由版本化规则配置负责。

## 6. 候选关系与正式关系

正式关系：

```text
CrossSection -[:MATCHES_SECTION_CAPTION]-> BlockCaption
```

候选关系：

```text
CrossSection -[:CANDIDATE_MATCHES_SECTION_CAPTION]-> BlockCaption
```

关系语义：

> 某个物理剖面标记经过文字识别和标签规范化后，与某个图块标题表达同一个逻辑断面标签。

### 6.1 候选边不等于正式事实

当前规划可以提前确定：

- 关系名称；
- 起点和终点；
- 关系语义；
- 查询方向；
- 未来属性；
- 统一输出字段；
- 写入前置条件。

在没有可比较识别证据前，不创建候选边或正式边。当双方已经具有可比较的 `TextObservation`，但候选不唯一、证据冲突、别名规则不明确或规则阈值不足时，只创建 `CANDIDATE_MATCHES_SECTION_CAPTION`。

候选边是一次具体关系判断的保存形式，不是正式图纸事实，也不需要额外建立 `RelationAssessment` 节点。候选边建议记录：

- `status`：`candidate`、`llm_reviewing`、`ambiguous`、`promoted`、`rejected` 或 `stale`；
- `candidate_count`；
- `score`；
- `conflict_reason`；
- `rule_version`；
- `observation_ids`；
- `alias_rule_id` 和 `alias_rule_version`，未使用别名规则时为空；
- `review_method`、`review_run_id`、`review_model_version` 和 `review_prompt_version`；
- `review_score`、`review_reason` 和 `reviewed_at`；
- `created_at` 和 `updated_at`。

### 6.2 正式关系的写入条件

第一版采用“确定性规则自动确认、多模态大模型独立复核、人工最终兜底”的策略。只有同时满足以下条件，才能由确定性规则直接建立正式关系：

1. `CrossSection` 和 `BlockCaption` 都存在可追溯的 `TextObservation`。
2. 双方原始文字均可规范化为非 `unknown` 的断面标签逻辑键。
3. 双方 `normalized_section_key` 相同；如果符号体系不同，则必须存在适用范围明确且已经确认的别名规则。
4. `CrossSection` 与候选 `BlockCaption` 位于同一 `DrawingPage`。
5. 候选目标唯一，不存在同等证据的多个 `BlockCaption`。
6. 当前查询没有字符冲突、裁剪缺失或模型调用失败。
7. 候选标题通过 `HAS_CAPTION` 唯一关联到目标 `DrawingBlock`，不存在空间关系冲突。

全部条件满足时，可由确定性规则自动写入 `MATCHES_SECTION_CAPTION`，不要求用户逐条确认。任一唯一性或冲突条件不满足时，先写入或更新 `CANDIDATE_MATCHES_SECTION_CAPTION`，再启动独立多模态复核，而不是直接要求人工处理。

正式关系建议记录：

- `status`，固定为 `confirmed`；
- `confirmation_method`：`deterministic_rule`、`multimodal_llm` 或 `human`；
- `confirmed_at`；
- `recognition_run_id`；
- `cross_section_observation_id`；
- `caption_observation_id`；
- `cross_section_label_system`；
- `cross_section_canonical_label`；
- `cross_section_section_key`；
- `caption_label_system`；
- `caption_canonical_label`；
- `caption_section_key`；
- `matched_section_key`；
- `alias_rule_id`，未使用别名规则时为空；
- `alias_rule_version`，未使用别名规则时为空；
- `rule_version`。

候选边被确认后，可以将其标记为 `promoted` 后保留审计记录，也可以在审计存储已经完整保留历史的前提下删除；不得让有效候选边与正式边同时表现为两个相互矛盾的当前结论。

### 6.3 候选关系的多模态独立复核

候选边不是人工任务的同义词，而是第二阶段裁决的输入。复核模型应在一次请求中比较同一关系问题下的全部候选，不能分别询问每个候选“是否匹配”，避免多个候选分别得到肯定答案。

复核输入至少包括：

- 原始来源元素裁剪图；
- 全部候选目标裁剪图；
- 候选关联的完整 `DrawingBlock` 裁剪图；
- 当前 `DrawingPage` 全局缩略图和各对象 bbox；
- 双方原始 `TextObservation`、规范化逻辑键和字符歧义；
- bbox 包含、重叠、标题归属等空间证据；
- 当前匹配规则版本和适用的图谱外部别名规则配置。

第二阶段复核必须使用独立 `review_run_id`。它可以使用同一基础模型，但必须是不同于首次识别的独立调用、复核提示词和更完整的候选上下文；条件允许时可以使用不同模型或多次一致性复核进一步降低自证风险。

多模态复核只允许返回三类结构化结论：

| 结论 | 图谱处理 |
|---|---|
| `accepted` | 唯一选择一个候选，且不违反硬性规则；建立正式关系，`confirmation_method=multimodal_llm`，原候选边标记为 `promoted` |
| `rejected` | 候选与视觉或上下文证据不符；候选边标记为 `rejected` 并记录原因 |
| `unresolved` | 多个候选证据相当、原图缺失或存在真实冲突；候选边保持 `ambiguous`，不建立正式关系 |

模型给出唯一选择还不足以无条件写入正式边。写入前必须再次校验同页范围、关系方向、候选集合完整性、结构化输出合法性以及硬性规则冲突。只有模型唯一选择与这些约束同时成立，才能提升为正式关系。

三个候选关系使用相同机制：

- `CANDIDATE_MATCHES_SECTION_CAPTION`：同时比较剖面标记、全部同页标题、关联图块和页面上下文；
- `CANDIDATE_CAPTION_OF`：同时比较标题和全部候选图块的构图、距离、边界与语义内容；
- `CANDIDATE_HAS_SECTION_MARK`：同时比较剖面标记与全部重叠或包含候选图块。

人工复核不是默认步骤。只有模型返回 `unresolved`，并且业务必须得到唯一关系时，才提交人工处理；如果当前查询允许返回不确定结果，则保持 `ambiguous` 即可。

## 7. 方案 C：按需识别并逐步缓存

### 7.1 核心原则

- 不预先对全部图纸执行 OCR。
- 查询先使用已有图谱缩小候选范围。
- 仅对本次查询需要的 `CrossSection`、标题、基础信息、表格、普通文字或 `DrawingBlock` 裁剪区域调用多模态模型。
- 模型输出作为 `TextObservation` 缓存，不直接视为正式语义关系。
- 基础信息、表格和图块的结构化解析分别作为 `BasicInfoInterpretation`、`TableInterpretation` 和 `BlockInterpretation` 缓存，不直接视为来源事实。
- 后续相同输入优先复用有效缓存。
- 证据变化、图片变化或模型版本变化时重新识别。

### 7.2 查询处理流程

1. 解析用户查询范围。
2. 从来源事实层定位 `Project`、`DrawingSet`、`DrawingPage` 或 `DrawingBlock`。
3. 从空间关系层查询来源图块关联的 `CrossSection`。
4. 根据原图路径和 bbox 生成 `CrossSection` 裁剪区域。
5. 检查是否存在可复用的 `TextObservation`。
6. 没有有效缓存时调用多模态模型，分别识别 `CrossSection` 上下两端或左右两端的标签。
7. 根据两个端点标签、符号体系、相对位置和排列方向生成 `canonical_label` 与 `normalized_section_key`。
8. 在同一 `DrawingPage` 内获取候选 `BlockCaption`。
9. 优先复用候选标题已有的 `TextObservation`；没有缓存时按需裁剪并识别。
10. 从标题文字中提取成对核心标签，识别符号体系并生成逻辑键。
11. 按 `normalized_section_key` 比较 `CrossSection` 和 `BlockCaption`；跨符号体系时只读取图谱外部已确认且作用域匹配的别名规则配置。
12. 通过现有 `DrawingBlock -[:HAS_CAPTION]-> BlockCaption` 关系找到目标图块。
13. 形成统一输出。
14. 缓存本次识别证据。
15. 候选唯一、确定性规则全部满足且没有冲突时，自动建立正式 `MATCHES_SECTION_CAPTION`；否则建立或更新 `CANDIDATE_MATCHES_SECTION_CAPTION`。
16. 对全部候选执行一次独立多模态复核，同时提供原始裁剪、完整候选集合、页面上下文、空间证据和已有观察。
17. 模型返回 `accepted` 且通过硬性规则校验时提升正式关系；返回 `rejected` 时失效对应候选；返回 `unresolved` 时保持 `ambiguous`。
18. 只有 `unresolved` 且业务必须取得唯一答案时，才进入人工复核。

### 7.3 图块解析流程

1. 根据查询返回的 `DrawingBlock` 获取图片路径和 bbox。
2. 检查是否存在图片哈希、bbox、模型、提示词和数据契约版本均匹配的有效 `BlockInterpretation`。
3. 存在有效缓存时，直接复用解析结果。
4. 没有有效缓存时，裁剪完整图块并调用多模态模型。
5. 模型同时输出结构化字段和自然语言总结。
6. 图块内部文字拆分为带局部 bbox 的 `TextObservation`。
7. 结构化完整结果写入不可变 JSON 产物，并生成 `payload_ref`。
8. 创建新的 `BlockInterpretation`，记录状态、摘要、检索字段、版本、证据和缓存键。
9. 查询服务将图块来源事实、空间关系和 AI 解析统一返回。
10. 解析失败或部分成功时仍返回图块来源事实，不用猜测值补齐缺失字段。

### 7.4 基础信息与其他页面元素解析流程

1. 查询 `DrawingBlock` 时，先沿所属 `DrawingPage` 查找本页 `HAS_BASIC_INFO`。
2. 本页缺失基础信息时，按图纸组规则查找首尾锚点；不使用“固定继承上一页”的规则。
3. 只有首尾公共字段一致或单一锚点可靠时，才形成页面级 `USES_BASIC_INFO` 候选或关系。
4. 按需裁剪 `DrawingBasicInfo` 并生成或复用 `BasicInfoInterpretation`，向用户返回具体字段、原文、摘要和不确定性。
5. 返回 `Table` 时按需生成或复用 `TableInterpretation`，返回表头、单元格结构、文字和摘要。
6. 返回 `BlockCaption`、`TableCaption`、`Title`、`PlainText` 或 `DrawingAnnotation` 时，生成或复用对应 `TextObservation`，返回识别文字而非只返回 ID。
7. 结构无法唯一确定时保留全部候选解释并返回 `ambiguous`；只是字段缺失且不存在冲突时返回 `partial`。

### 7.5 候选范围

默认候选范围严格限定在同一 `DrawingPage`。即使同一 `DrawingSet` 的其他页面存在相同的 `BlockCaption` 标题，例如 `A-A`、`B-B`、`Ⅰ-Ⅰ` 或 `1-1`，也不得进入本次候选集合。

候选检索规则：

1. 只读取当前 `CrossSection.page_id` 对应页面内的 `BlockCaption`。
2. 不搜索同一 `DrawingSet` 的其他页面。
3. 如果当前页面存在多个同名标题，全部保留为候选，不使用 bbox 距离、稳定 ID 或模型自由推理强制选择唯一结果。

未来如需跨页面关联，必须新增独立、明确且可审计的跨页规则，不沿用本方案的默认匹配。

### 7.6 缓存策略

缓存键至少由以下内容共同确定：

- 图片内容哈希；
- 元素稳定业务 ID；
- 裁剪 bbox；
- 识别或解析任务类型；
- 模型名称和版本；
- 提示词版本；
- 图像预处理版本；
- 规范化规则版本；
- 对应语义解析的数据契约版本。

缓存键是 `TextObservation` 或各类 `Interpretation` 的属性，不是图节点，也不是加密密钥。它是上述输入条件经过稳定序列化和哈希后形成的结果版本指纹。

原始文字观察缓存、结构化解析缓存和关系匹配缓存应当分开：

- `TextObservation` 缓存不包含别名规则版本；
- `Interpretation` 缓存包含对应数据契约版本；
- 关系匹配缓存包含双方观察版本、候选范围、匹配规则版本以及适用的 `alias_rule_version`。
- 多模态候选复核缓存还必须包含完整候选集合指纹、页面上下文图像哈希、复核模型版本和复核提示词版本；候选集合发生变化时复核缓存失效。

以下情况缓存失效：

- 原图内容发生变化；
- bbox 发生变化；
- 模型或提示词版本发生变化；
- 规范化规则发生不兼容变化；
- 图谱外部别名规则配置发生变化时，匹配结果缓存失效，但原始 `TextObservation` 仍可复用；
- 图块解析数据契约发生不兼容变化；
- 用户将观察标记为错误。

缓存命中只表示可以复用已有模型观察，不表示该观察已经变成正式图谱事实。

## 8. 完整证据路径

统一的业务证据路径为：

```text
来源 DrawingBlock
  -[:HAS_SECTION_MARK]->
物理 CrossSection
  -[:HAS_OBSERVATION]-> TextObservation
  -> 符号体系 + 规范值 + 逻辑键
  -> CANDIDATE_MATCHES_SECTION_CAPTION 或 MATCHES_SECTION_CAPTION
  -> BlockCaption
  <-[:HAS_CAPTION]-
目标 DrawingBlock
```

图块解析使用并行的证据路径：

```text
DrawingPage
  -[:HAS_BLOCK]->
DrawingBlock
  -[:HAS_INTERPRETATION]->
BlockInterpretation
  -> TextObservation / payload_ref
  -> 构件、内部文字、尺寸、材料、构造特征、空间关系和自然语言总结
```

基础信息使用页面级证据路径：

```text
DrawingBlock
  <-[:HAS_BLOCK]-
DrawingPage
  -[:HAS_BASIC_INFO|USES_BASIC_INFO]->
DrawingBasicInfo
  -[:HAS_INTERPRETATION]->
BasicInfoInterpretation
  -> 原始文字、结构化字段、摘要、状态和证据
```

其他页面元素使用以下通用路径：

```text
DrawingPage
  -> Table / Caption / Title / PlainText / DrawingAnnotation
  -> TableInterpretation 或 TextObservation
  -> 结构化内容或可读文字、状态和证据
```

其中：

- 来源图块是剖面标记几何上所在的图块。
- 目标图块是对应断面标题所描述的图块。
- 来源图块和目标图块可以是同一个，也可以是同一页面内的不同图块；本方案不自动跨页匹配断面标题。
- “来源”和“目标”只是本次断面查询中的角色，不改变节点本身类型。
- 稳定业务 ID 用于引用和追溯；真正面向用户的回答必须组合对应语义内容。

## 9. 统一输出契约

每次断面匹配查询或图块返回查询统一返回以下信息。

### 9.1 查询范围

- `project_id`
- `drawing_set_id`
- 输入的 `page_id` 或 `block_id`
- 实际搜索过的页面 ID

### 9.2 来源图块

- `source_block_id`
- `page_id`
- `page_number`
- `image_path`
- `bbox`
- `normalized_bbox`

### 9.3 物理剖面标记

- `cross_section_id`
- `bbox`
- `raw_text`
- `label_system`
- `canonical_label`
- `normalized_section_key`
- `marker_components`
- `component_count`
- `pair_orientation`
- `observation_id`
- `recognition_status`

当一个 `CrossSection` 区域的上下两端或左右两端各有一个相同标签，例如 `A`、`A` 或 `Ⅰ`、`Ⅰ` 时，仍只返回一条 `CrossSection` 记录。`marker_components` 分别返回两个端点的文字、局部 bbox 和相对位置，`component_count` 返回端点数量，`pair_orientation` 返回 `vertical` 或 `horizontal`；这些字段都不会改变节点数量。`raw_text` 只能保存模型实际返回的原始内容，不能人为拼接成图中不存在的 `A-A` 或 `Ⅰ-Ⅰ`。

### 9.4 匹配标题

- `block_caption_id`
- `page_id`
- `bbox`
- `raw_text`
- `extracted_pair`
- `label_system`
- `canonical_label`
- `normalized_section_key`
- `observation_id`
- `recognition_status`

### 9.5 目标图块

- `target_block_id`
- `page_id`
- `page_number`
- `image_path`
- `bbox`
- `normalized_bbox`

### 9.6 AI 图块解析

- `interpretation_id`
- `summary`
- `interpreted_type`
- `components`
- `internal_annotations`
- `construction_features`
- `spatial_relations`
- `materials`
- `dimensions`
- `uncertainties`
- `analysis_status`
- `payload_ref`
- `recognition_run_id`
- 模型名称和版本
- 提示词版本
- 数据契约版本
- 是否使用缓存

`interpreted_type`、构件、构造和空间关系必须明确标记为 AI 解析，不得混入 `DrawingBlock` 来源事实字段。没有有效解析时返回 `not_interpreted`，不能用空字符串或猜测内容伪装成已解析。

### 9.7 页面基础信息

- `basic_info_id`
- `source_page_id`
- `context_source`：`current_page`、`group_start` 或 `group_end`
- `group_id`
- `raw_text`
- 项目名称、图名、专业、图号、比例、设计、审核、日期等已识别字段
- `summary`
- `uncertainties`
- `interpretation_id`
- `analysis_status`
- 模型、提示词和数据契约版本
- 是否使用缓存

查询某个图块时，这些字段通过所属页面取得。输出中不得只给出 `basic_info_id` 而省略已有的文字与结构化内容。

### 9.8 其他页面元素语义内容

- 元素稳定业务 ID、类型、所属页面和 bbox
- 标题、注释和普通文字的原始识别文字及规范化文字
- 表格的标题、表头、行列、单元格内容和摘要
- 对应 `TextObservation` 或 `TableInterpretation` ID
- 识别或解析状态、不确定性、模型版本和缓存信息

如果尚未执行识别，返回 `not_interpreted` 或 `not_recognized`，不能把“只有 ID”包装成完整回答。

### 9.9 匹配结论

统一状态：

| 状态 | 含义 |
|---|---|
| `confirmed` | 正式关系已经成立；通过 `confirmation_method` 区分确定性规则、多模态模型和人工确认 |
| `candidate` | 已建立 `CANDIDATE_*` 候选边，等待或正在进行独立多模态复核 |
| `partial` | 两个端点只识别出一部分，或其他证据缺失 |
| `ambiguous` | 存在两个或更多合理解释，且现有证据不能唯一决定 |
| `not_found` | 没有找到相同逻辑键或已确认别名键的标题 |
| `recognition_failed` | 图片、裁剪或模型调用失败 |

`partial` 与 `ambiguous` 必须区分：单纯缺字段、只识别到一个端点或只有一个可靠基础信息锚点属于 `partial`；存在相互冲突或多个同等合理解释才属于 `ambiguous`。

匹配结论还应返回：

- `cross_section_section_key`
- `caption_section_key`
- `matched_section_key`
- `applied_alias_rule_id`，未使用别名规则时为空
- `applied_alias_rule_version`，未使用别名规则时为空
- `relation_type`，例如 `CANDIDATE_MATCHES_SECTION_CAPTION` 或 `MATCHES_SECTION_CAPTION`
- `confirmation_method`，候选关系时为空
- `review_status`：`not_started`、`reviewing`、`accepted`、`rejected` 或 `unresolved`

### 9.10 推理与审计信息

- `recognition_run_id`，用于回查图谱外部运行日志
- `review_run_id`、复核模型版本和复核提示词版本
- 模型复核结论、分数和理由
- 模型名称和版本
- 提示词版本
- 规范化规则版本
- 应用的别名规则 ID 与版本
- 是否使用缓存
- 候选数量
- 警告列表
- 是否已写入正式关系

输出不得包含 Neo4j 内部节点 ID，也不得在 `candidate`、`partial` 或 `ambiguous` 状态下声称已经找到确定对应关系。

## 10. 异常与歧义处理

### 10.1 图片或裁剪失败

- 返回 `recognition_failed`。
- 保留图谱中已有的来源图块和 `CrossSection` 信息。
- 不创建文字观察和语义关系。

### 10.2 模型无法识别

- 原始文字允许为空。
- 规范化结果设为 `unknown`。
- 返回 `partial` 或 `recognition_failed`。
- 不建立候选边或正式关系。

### 10.3 两个端点识别不完整

- 保留原有的单个 `CrossSection` 节点。
- 返回已经识别到的端点标签及其位置。
- 记录另一个端点未识别或不可见的警告。
- 匹配状态最高只能为 `partial`，不能自动成为正式关系。

### 10.4 同一物理区域内的端点标签冲突

例如一个 `CrossSection` 区域上端识别为 `A`、下端识别为 `B`，或左端识别为 `Ⅰ`、右端识别为 `Ⅱ`：

- 保留一个 `CrossSection` 节点和一个包含冲突分量的 `TextObservation`。
- 返回 `ambiguous`。
- 不选择标题。
- 不创建正式关系。

### 10.5 多个同名标题

- 返回全部候选 `BlockCaption` 和对应目标图块。
- 不使用稳定 ID、页面顺序或模型自由推理强制选出一个目标。
- 为每个仍具有合理证据的目标建立或更新 `CANDIDATE_MATCHES_SECTION_CAPTION`，并在边属性中记录候选数量和冲突原因。
- 将全部候选一次性交给独立多模态复核；模型能够唯一选择且通过硬性规则校验时建立正式关系。
- 模型返回 `unresolved` 时状态才保持为 `ambiguous`，必要时再人工复核。

### 10.6 已确认关系与新识别冲突

- 保留原有正式关系。
- 将新结果标记为冲突观察。
- 先由独立多模态复核比较原正式关系与新候选，并返回警告和结构化判断。
- 多模态复核不得自动覆盖或删除已有正式关系；冲突候选使用 `status=ambiguous` 表达。确需改变已有正式关系时，再由人工最终确认。

### 10.7 多模态服务不可用

- 查询退化为当前图谱已有的来源事实和空间关系结果。
- 明确返回语义识别未执行。
- 不把“未执行”表达为“没有对应标题”。

### 10.8 图块解析部分成功或失败

- 图块来源事实和空间关系始终正常返回。
- 只识别出部分构件或文字时，`analysis_status` 返回 `partial`。
- 构件、文字或空间关系之间存在明显冲突时，返回 `ambiguous`。
- 模型调用、裁剪或结构化结果校验失败时，返回 `failed`。
- 缺失字段保持缺失，并写入 `uncertainties` 或警告列表，不由系统猜测补齐。

### 10.9 图块解析缓存失效

- 原图、bbox、模型、提示词或数据契约变化时，旧解析标记为 `stale`。
- `stale` 解析可以保留用于审计，但不得作为默认最新解析返回。
- 新解析创建新节点，不覆盖旧 `BlockInterpretation`。

### 10.10 `ambiguous` 状态清单

以下情况应明确输出 `ambiguous`，并保留候选与冲突证据：

| 编号 | 场景 | 典型表现 | 处理原则 |
|---|---|---|---|
| A1 | 剖面端点标签冲突 | 同一 `CrossSection` 的两端分别识别为 `A` 和 `B`，或 `Ⅰ` 和 `Ⅱ` | 保留一个物理节点，不生成逻辑键或标题匹配 |
| A2 | 字符或符号体系存在多个合理解释 | 模糊字符可能是字母 `I`、罗马数字 `Ⅰ` 或阿拉伯数字 `1`，现有图像无法排除其中一种 | 返回所有带符号体系的规范化候选，不强制选值 |
| A3 | 同页存在多个同逻辑键断面标题 | 同一 `DrawingPage` 中有多个可规范化为 `SECTION_ALPHA_A` 的 `BlockCaption` | 为各标题建立 `CANDIDATE_MATCHES_SECTION_CAPTION` 并统一交给多模态复核；仍无法唯一选择时返回 `ambiguous` |
| A4 | 标题与图块空间关联不唯一 | 一个 `BlockCaption` 对多个 `DrawingBlock` 具有同等空间证据 | 为各候选建立 `CANDIDATE_CAPTION_OF` 并由多模态模型比较完整图块上下文；无法裁决时不建立唯一 `HAS_CAPTION` |
| A5 | 图纸组首尾基础信息冲突 | 首张与末张图纸的项目、专业、图名或图号公共字段不一致 | 不向中间页面传播基础信息，要求复核组边界或锚点 |
| A6 | 基础信息图纸组边界不唯一 | 某个 block 页面同时符合两组基础信息，或仅靠连续页码无法确定归属 | 返回候选组及证据，不创建确定 `USES_BASIC_INFO` |
| A7 | 同一页面多份基础信息互相冲突 | 本页多个 `DrawingBasicInfo` 对同一字段给出不同值，且无法判断主次 | 保留全部解释，字段不合并成一个确定值 |
| A8 | 表格结构存在多个合理解释 | 行列边界、跨行跨列或合并单元格有两种以上合理解析 | 保存候选结构，不把任一结构表述为确定事实 |
| A9 | 图块内部解析相互冲突 | 构件类型、文字、尺寸或空间关系不能同时成立 | 在 `BlockInterpretation.uncertainties` 中说明冲突，不补猜 |
| A10 | 多个有效模型观察互相冲突 | 相同图片与有效范围下，不同模型或版本给出不同文字或结构 | 保留版本化观察，默认结果不得伪装为唯一答案 |
| A11 | 新识别与正式关系冲突 | 新结果的符号体系、逻辑键或别名规则与已有自动、模型或人工确认关系不一致 | 保留正式关系和冲突候选边，先由独立多模态复核；模型不得自动覆盖已有正式边，确需变更时人工兜底 |
| A12 | 跨符号体系别名不唯一或未确认 | `I-I`、`Ⅰ-Ⅰ`、`1-1` 可能被解释为同一断面，但没有明确适用规则，或多条别名规则互相冲突 | 多模态模型可结合明确图纸说明复核外部规则配置；没有明确依据时不跨体系匹配并保持 `ambiguous` |

以下情况本身不属于 `ambiguous`：

- 只识别出一个剖面端点，但不存在相互冲突的第二种解释：`partial`。
- 只有一个可靠的基础信息首端或末端锚点，另一端缺失：`partial`。
- 基础信息、表格或图块解析只缺少部分字段，但已有字段之间不冲突：`partial`。
- 图片读取、裁剪、模型调用或结构化校验失败：`recognition_failed` 或 `failed`。
- 没有找到相同逻辑键或已确认别名键的标题：`not_found`。
- 尚未执行识别或解析：`not_recognized` 或 `not_interpreted`。

## 11. 当前状态与目标状态

### 11.1 当前已经具备

- 来源事实层的项目、图纸册、页面、图块和页面元素。
- 页面级来源关系。
- 当前实现中已有 block 级 `HAS_CAPTION`、`HAS_BASIC_INFO`、`HAS_ANNOTATION`、`HAS_SECTION_MARK`。
- 稳定业务 ID、bbox、图片路径和导入批次审计。
- 图块追溯查询和 block 级关系查询。

当前代码仍会把本页基础信息，或特定流程中的同一 `DrawingSet` 上一页基础信息，批量关联到当前页每个 block；现有查询主要返回 `basic_info_ids`。这只是“当前实现证据”，不是本方案认可的目标语义。目标状态将弃用 block 级 `HAS_BASIC_INFO`，改为页面级 `HAS_BASIC_INFO` / `USES_BASIC_INFO` 路径，并返回基础信息的识别内容。

### 11.2 当前尚未具备

- 图谱外部识别运行日志及 `recognition_run_id` 回查机制
- `TextObservation`
- `BlockInterpretation`
- `BasicInfoInterpretation`
- `TableInterpretation`
- 图谱外部版本化 `SectionLabelAliasRule` 配置
- 多模态模型调用编排
- 裁剪与缓存管理
- 通用断面标签规范化
- 符号体系识别和有作用域的别名规则
- 统一断面语义查询
- 候选关系的独立多模态复核流程及人工最终兜底机制
- `CANDIDATE_MATCHES_SECTION_CAPTION`、`CANDIDATE_CAPTION_OF` 和 `CANDIDATE_HAS_SECTION_MARK` 候选边
- 正式 `MATCHES_SECTION_CAPTION` 写入
- 图块构件与内部文字结构化解析
- 图块自然语言总结
- 图块解析缓存、版本和统一返回
- 页面级基础信息组锚点判定与 `USES_BASIC_INFO`
- 标题、基础信息、表格、注释和普通文字的语义内容统一返回

### 11.3 目标状态

在不破坏现有两层图谱的前提下，新增独立语义证据层，使系统能够按需完成：

```text
CrossSection 图像识别
  -> 单节点内上下端或左右端标签解析
  -> 符号体系、规范值和逻辑键生成
  -> BlockCaption 候选识别
  -> 同键匹配或已确认别名匹配
  -> 目标 DrawingBlock 定位
  -> 统一证据输出
  -> 逐步缓存
  -> 唯一、无冲突时由确定性规则自动写入正式关系
  -> 多候选或冲突时写入 CANDIDATE_* 并进入独立多模态复核
  -> 模型仍 unresolved 且业务必须唯一时才进入人工复核
```

同时新增独立的图块解析支线：

```text
DrawingBlock 图像裁剪
  -> 构件与内部文字识别
  -> 尺寸、材料、构造特征和空间关系解析
  -> 结构化结果校验
  -> 自然语言总结
  -> BlockInterpretation 与 TextObservation
  -> 逐步缓存
  -> 随 DrawingBlock 统一返回
```

基础信息采用页面级上下文支线：

```text
存在 DrawingBlock 的 DrawingPage
  -> 优先读取本页 DrawingBasicInfo
  -> 本页缺失时识别候选图纸组首尾锚点
  -> 比较项目、专业、图名、图号等公共字段
  -> 唯一可靠时建立或返回页面级 USES_BASIC_INFO
  -> 生成或复用 BasicInfoInterpretation
  -> 返回稳定 ID、原文、结构化字段、摘要和状态
```

不存在 block 的页面不要求补充基础信息；如果该页确有原始 `DrawingBasicInfo` 标注，仍保留来源事实。

## 12. 实施阶段建议

本文只确定阶段，不展开代码任务。

### 阶段一：语义证据数据契约

- 定义图谱外部识别运行日志及 `recognition_run_id` 回查机制。
- 定义 `TextObservation`。
- 定义 `BlockInterpretation`。
- 定义 `BasicInfoInterpretation`。
- 定义 `TableInterpretation`。
- 定义来源元素 `-[:HAS_OBSERVATION]-> TextObservation`。
- 定义各类 `Interpretation -[:SUPPORTED_BY]-> TextObservation`。
- 定义图谱外部 `SectionLabelAliasRule` 版本化配置的作用域、状态和审计字段。
- 定义缓存键和失效条件。
- 定义统一查询输出状态。

### 阶段二：CrossSection 按需识别

- 从已有图谱定位 `CrossSection` 物理区域。
- 按 bbox 裁剪。
- 调用可替换的多模态模型。
- 在单个节点内分别解析上下两端或左右两端的标签，不拆分节点。
- 保存原始观察和运行证据。

### 阶段三：通用断面标签规范化

- 从 `CrossSection` 两个空间端点提取标签，从 `BlockCaption` 完整标题中提取成对核心标签。
- 区分 `alphabetic`、`roman`、`numeric`、`alphanumeric` 和 `unknown` 符号体系。
- 在同一符号体系内统一大小写、全半角、横线、空格和允许的格式差异。
- 使用“符号体系 + 规范值”生成 `normalized_section_key`。
- 支持 `A-A`、`B-B`、`C-C`、`Ⅰ-Ⅰ`、`1-1`、`A1-A1` 等形式，示例不构成封闭枚举。
- 默认不合并 `I-I`、`Ⅰ-Ⅰ` 和 `1-1`。
- 在图谱外部定义有明确作用域、依据、版本和确认状态的跨符号体系别名规则配置。
- 对未知、冲突和别名不唯一结果保守返回。

### 阶段四：BlockCaption 按需识别与缓存

- 只在同一 `DrawingPage` 内读取候选标题。
- 复用有效缓存。
- 按需识别未缓存标题。

### 阶段五：统一匹配查询

- 组合来源图块、物理标记、符号体系、规范值、逻辑键、标题和目标图块。
- 返回统一状态和完整证据。
- 定义并写入 `CANDIDATE_MATCHES_SECTION_CAPTION`、`CANDIDATE_CAPTION_OF` 和 `CANDIDATE_HAS_SECTION_MARK` 候选边。
- 候选边保存状态、候选数量、分数、冲突原因、规则版本和观察引用，不建立 `RelationAssessment` 节点。

### 阶段六：自动确认、多模态复核与正式关系

- 候选唯一、规则条件全部满足且不存在冲突时，自动写入 `MATCHES_SECTION_CAPTION`。
- 多候选、证据冲突、跨符号体系或规则边界不明确时，启动独立多模态复核。
- 复核模型必须一次比较全部候选并返回 `accepted`、`rejected` 或 `unresolved` 的结构化结果。
- `accepted` 且通过硬性规则校验时，将对应候选提升为正式关系，`confirmation_method=multimodal_llm`。
- `unresolved` 时保持 `ambiguous`；只有业务必须得到唯一结果时才提供人工复核入口。
- 正式边保存 `confirmation_method`、确认时间、规则版本、观察引用和可选别名规则引用。

### 阶段七：图块按需解析

- 定义结构化解析数据契约和不可变 JSON 产物格式。
- 对 `DrawingBlock` bbox 执行按需多模态解析。
- 输出构件、内部文字、尺寸、材料、构造特征和空间关系。
- 生成自然语言总结、解析状态和不确定性。
- 写入版本化 `BlockInterpretation`，并关联内部文字 `TextObservation`。
- 扩展统一查询，使返回图块时同时返回有效 AI 解析。

### 阶段八：DrawingBasicInfo 页面级上下文调整

- 停止新增 `DrawingBlock -[:HAS_BASIC_INFO]-> DrawingBasicInfo` 批量关系。
- 保留 `DrawingPage -[:HAS_BASIC_INFO]-> DrawingBasicInfo` 来源事实。
- 定义图纸组候选范围、公共字段比较和首尾锚点规则。
- 对存在 block 且本页缺失基础信息的页面，生成可审计的页面级 `USES_BASIC_INFO`。
- 明确当前页优先、单锚点 `partial`、锚点冲突或组边界不唯一 `ambiguous`。
- 查询图块时经所属页面取得基础信息，并返回 `BasicInfoInterpretation` 内容。
- 现有 block 级基础信息关系的迁移或清理须另行制定可回滚的数据迁移方案。

### 阶段九：其他页面元素按需解析

- 为标题、注释和普通文字生成或复用 `TextObservation`。
- 为表格生成或复用 `TableInterpretation`。
- 统一返回稳定业务 ID、识别内容、结构化结果、摘要、状态和证据。

## 13. 验收标准

### 13.1 架构边界

- 页面级来源关系全部归入来源事实层。
- block 级标题、注释和剖面标记派生关系归入空间与上下文派生关系层。
- 基础信息只保留页面级来源或上下文关系，不向每个 block 批量建立 `HAS_BASIC_INFO`。
- 多模态文字观察和匹配归入语义证据层。
- `RecognitionRun` 和 `SectionLabelAliasRule` 不作为核心知识图谱节点，分别位于运行日志层和规则配置层。
- 不建立 `RelationAssessment` 节点；未确认判断以 `CANDIDATE_*` 边表达。
- `BlockInterpretation` 归入语义证据层，不新增第四个顶层架构层。
- `BasicInfoInterpretation` 和 `TableInterpretation` 归入语义证据层。
- AI 解析不得覆盖 `DrawingBlock` 来源事实或写入 `DrawingBlock.block_type`。
- 三层数据不互相覆盖。

### 13.2 真实性

- 未识别时不存在具体候选边或正式 `MATCHES_SECTION_CAPTION`。
- 只有确定性规则全部满足且无冲突，或独立多模态复核唯一确认并通过硬性规则校验，或人工最终确认时，才存在正式 `MATCHES_SECTION_CAPTION`。
- 候选结果始终使用 `CANDIDATE_*` 关系并带有非确定状态。
- 模型观察始终可以通过 `recognition_run_id` 追溯到外部运行日志、图片和 bbox。
- 多模态候选复核始终可以通过独立 `review_run_id` 追溯到完整候选集合、复核模型和提示词版本。
- 未确认或超出适用范围的图谱外部别名规则配置不参与确定匹配。

### 13.3 规范化

- `CrossSection` 两端的 `A`、`A` 与 `BlockCaption` 的 `A-A剖面` 可以映射到 `SECTION_ALPHA_A`。
- `CrossSection` 两端的 `Ⅰ`、`Ⅰ` 与 `BlockCaption` 的 `Ⅰ-Ⅰ` 可以映射到 `SECTION_ROMAN_1`。
- `CrossSection` 两端的 `1`、`1` 与 `BlockCaption` 的 `1-1` 可以映射到 `SECTION_NUMERIC_1`。
- `A-A`、`B-B`、`C-C`、罗马数字、阿拉伯数字和字母数字组合均使用同一通用机制，而不是分别硬编码。
- `I-I`、`Ⅰ-Ⅰ` 和 `1-1` 默认生成不同逻辑键。
- `01-01` 与 `1-1` 默认生成不同逻辑键，前导零不会被静默删除。
- `A-B` 或两个物理端点不一致时不能生成确定逻辑键。
- 只有作用域明确且已确认的别名规则可以跨符号体系匹配。
- 一个 `CrossSection` 标注 bbox 始终对应一个节点。
- 同一 bbox 的上下两端或左右两端出现两个同名标签时，不拆分为两个节点。
- 两个端点的位置和排列方向可以独立记录，但不得伪造成原图中不存在的连字符文字。
- 只有两个独立标注 bbox 才对应两个独立节点。
- 未知字符或无法判断的符号体系不被强制映射。
- 冲突标记不会生成确定匹配。

### 13.4 缓存

- 相同图片、bbox、模型、提示词和规则版本能够复用缓存。
- 图片、bbox、模型或规则变化后缓存失效。
- 别名规则变化只使依赖它的匹配结果缓存失效，不使原始文字观察失效。
- 缓存复用不会改变结果的确认状态。
- 缓存键作为证据节点属性保存，不建立独立缓存节点。

### 13.5 统一输出

- 每次查询都能返回来源图块、物理标记、符号体系、规范值、逻辑键、标题候选、目标图块、匹配状态和审计信息。
- 返回 `DrawingBlock` 时能够同时返回有效的结构化 AI 解析和自然语言总结。
- 返回标题、基础信息、表格、注释和普通文字时，稳定 ID 与识别文字或结构化内容同时出现。
- 查询图块基础信息时，经所属页面返回本页或图纸组锚点的内容、来源和状态，不只返回 `basic_info_ids`。
- 缺失或失败字段使用明确状态表达，不用猜测值填充。
- 输出只使用稳定业务 ID，不暴露 Neo4j 内部 ID。

### 13.6 图块解析

- 返回图块时可以同时返回结构化解析和自然语言总结。
- 构件、内部文字、尺寸、材料、构造特征和空间关系均有明确字段。
- `interpreted_type` 只存在于 `BlockInterpretation`，不写入 `DrawingBlock.block_type`。
- 每个解析结果可以追溯到图块图片、bbox、模型、提示词和数据契约版本。
- 相同有效输入能够复用缓存；输入或版本变化后旧解析进入 `stale`。
- `partial`、`ambiguous`、`failed` 和 `not_interpreted` 不会被表达为完整、确定的解析。

### 13.7 降级能力

- 多模态服务不可用时，仍能返回当前图谱已有的来源事实和空间关系。
- 语义识别未执行与“没有找到匹配”能够明确区分。

### 13.8 DrawingBasicInfo 上下文

- 没有 `DrawingBlock` 的页面不要求补充或继承基础信息。
- 存在 `DrawingBlock` 的页面优先使用本页实际基础信息。
- 本页缺失时，只在公共字段可验证的候选图纸组内使用首尾锚点。
- 连续页码不能单独证明图纸组归属。
- 单一可靠锚点返回 `partial`；锚点冲突或组边界不唯一返回 `ambiguous`。
- 页面内图块数量增加不会产生基础信息关系的笛卡尔积。

### 13.9 歧义状态

- 每个 `ambiguous` 结果至少返回两个冲突项或候选项及其证据。
- 单纯信息缺失不会误报为 `ambiguous`。
- `ambiguous` 状态下不写入确定语义关系，不覆盖来源事实或已有正式关系。
- 候选进入 `ambiguous` 前已经完成适用的独立多模态复核，或明确记录复核因服务不可用而未执行。

## 14. 非目标

本方案当前不包含：

- 全量离线 OCR 任务；
- 任意自然语言 Agent；
- HTTP/REST API；
- `NEAR` 空间关系网络；
- 向页面内每个 `DrawingBlock` 批量复制 `HAS_BASIC_INFO`；
- 仅凭上一页页码继承 `DrawingBasicInfo`；
- 将 AI 推断类型写入或覆盖 `DrawingBlock.block_type`；AI 类型只允许存在于 `BlockInterpretation.interpreted_type`；
- 跨 `DrawingPage` 自动断面匹配；
- 在证据不完整、候选不唯一或规则冲突时写入正式语义关系；
- 使用模型回答覆盖原始标注事实；
- 将所有用户问题和回答永久写入知识图谱。

## 15. 最终结论

最终图块图谱采用以下主线：

```text
来源事实层
  + 空间与上下文派生关系层
  + 按需多模态语义证据层
  + 逐步缓存
  + CANDIDATE_* 候选关系
  + 确定性规则或多模态复核确认后的正式关系
  + 仅在必要时使用的人工兜底
```

当前页面级关系和 block 级关系已经足以作为稳定底座，但不足以完成断面文字语义匹配。项目不需要现在建设全量离线 OCR，可以先采用查询时多模态识别，并将结果作为可追溯观察逐步缓存。

`DrawingBasicInfo` 在目标架构中是页面级上下文：没有 block 的页面不要求补充基础信息；有 block 的页面优先使用本页信息，本页缺失时再依据可验证的图纸组首尾锚点补充。图块通过所属页面取得基础信息，不再为每个 block 批量创建关系。

`MATCHES_SECTION_CAPTION` 只在物理标记、带符号体系的逻辑键、同页候选、目标图块和规则证据共同支持唯一结论时写入；确定性条件完整时可以自动确认。多候选、冲突或规则边界不明确时使用 `CANDIDATE_MATCHES_SECTION_CAPTION` 保存判断状态和证据，再由独立多模态模型一次性比较全部候选。模型唯一选择且通过硬性规则校验时可以确认正式关系；模型仍返回 `unresolved` 时保持 `ambiguous`，只有业务必须唯一时才人工兜底。跨符号体系的匹配还必须引用作用域明确、可审计且已确认的图谱外部别名规则配置。

图块返回能力同时扩展为“来源事实 + 空间关系 + AI 图块解析”。AI 解析通过版本化 `BlockInterpretation` 保存结构化字段和自然语言总结，内部文字复用 `TextObservation`，完整嵌套结果通过 `payload_ref` 引用。该设计使图块解析可缓存、可审计、可更新，同时不会把模型解释混入原始图纸事实。

同样，标题、基础信息、表格、注释和普通文字也必须返回可读语义内容。ID 只负责稳定引用和追溯，不能代替对图纸内容的识别与回答。

核心存储原则最终归纳为：图纸对象作为节点，已经成立的事实作为正式边，未确认的具体判断作为 `CANDIDATE_*` 候选边，模型观察与结构化解释作为证据节点，算法运行与编号别名规则分别留在图谱外部日志和版本化配置中。

# 产品实现层实施门槛表

**日期：** 2026-08-12
**适用范围：** 产品实现层 00-07 模块、问题理解闭环、后续 `DrawingAssistantService` 与产品级 adapter。
**用途：** 明确哪些能力当前不做、为什么不做、什么时候可以做、做到什么算通过，避免把“预留”“后续”“建议”误写成已经实施。

## 1. 基准原则

本表以当前项目架构为准：

- 现有稳定链路仍是 `QA adapter -> DrawingGraphQAService -> DrawingGraphToolFacade -> ports/services -> repository/Neo4j`。
- 产品公共合同与通用检索闭环已作为产品层底座落地：`assistant_models.py`、`GraphRetrievalService`、检索 planner/executor/projection 及 QA 兼容映射。
- 01 问题理解闭环已落地：`QuestionUnderstandingService` 及其规则路由、scope 解析、多意图拆分、证据模板、澄清策略、轻量追溯与受约束模型协议/fake/校验模块（Tasks 1-23）。
- 完整产品助手目标链路是 `AssistantRequest -> QuestionUnderstandingResult -> RetrievalBundle -> SemanticGapDecision -> RecognitionResult -> EvidenceBundle -> AnswerPackage -> TraceRecord / FeedbackEvent`。
- 默认 `write_back=false`。模型调用授权、写回授权、候选提升和正式关系建立必须分开判断。
- 候选关系、`matched_candidate`、模型观察和模型解释都不能冒充来源事实或正式关系。
- 单元测试、离线模型合同测试、live DashScope、live Neo4j 和写回验证必须分层报告。

## 2. 总览门槛表

| 能力 | 现在是否做 | 现在不做的原因 | 什么时候可以做 | 做到什么算通过 |
|---|---|---|---|---|
| 01 规则优先问题理解闭环 | 已作为 01 模块完成（Tasks 1-23） | 已完成；完整 `DrawingAssistantService` 与产品级 adapter 仍不做 | 已完成；后续仅按规则覆盖缺口扩展 | `AssistantRequest` 可稳定生成 `QuestionUnderstandingResult`；缺 scope 返回澄清；多意图拆分稳定；现有六类 QA 兼容（`diagnostic_status` 映射为 `drawing_diagnostic`）；不访问图谱、不调用 Qwen/真实模型、不写回；专项 122 项与全量 1279 项单元测试通过，live Neo4j 未验证 |
| 方案 B：受约束文本模型理解 | 协议、fake 与输出校验已实现；真实调用仍不做 | 规则合同、澄清机制和安全校验已稳定，但真实模型调用尚未进入本阶段范围 | 规则无法唯一分类、低置信度、多意图或口语表达成为主要瓶颈，且离线合同与 live 验证计划明确后 | 已有 `QuestionUnderstandingModelClient` 协议、fake client、字段白名单校验与脱敏测试；非法输出返回 `model_output_invalid`；真实调用需单独离线合同和 live 验证 |
| 02 通用图谱检索 | 已作为底座完成，后续按需扩展 | 当前不应重复实现或绕过已有 `GraphRetrievalService` | 当新的 `EvidenceRequirement` 不能被现有 facade 白名单满足时 | 只通过 `DrawingGraphToolFacade` 或新增受控只读 port；缺能力写入 `missing_evidence`；不拼 Cypher、不调用 Qwen、不创建 run log、不写 Neo4j |
| 03 语义缺口判断 | 当前问题理解首版不做 | 01 只声明证据需求，不能提前判断图谱证据是否足够或是否需要识别 | 01 输出稳定、02 能返回完整 `RetrievalBundle` 后 | 对每个 `EvidenceRequirement` 输出满足/缺失/过期/冲突判断；给出稳定 reason codes；缓存命中不创建识别目标；禁止识别或预算不足时不调用模型 |
| 04 Qwen 多模态识别运行编排 | 当前问题理解首版不做真实调用 | 识别必须由 03 的缺口判断触发，不能由问题理解或 adapter 直接触发 | 03 能生成最小 `RecognitionTarget`，且 image path、bbox、task type、模型 profile、prompt version 均可验证后 | 支持首版 task type；输出严格结构化 `RecognitionResult`；dry-run 无持久化副作用；API key 不进入 DTO/日志/命令；离线、live DashScope、live Neo4j 分开验收 |
| 04 写回语义证据 | 默认不做 | `allow_recognition=true` 不等于允许写数据库；写回会影响 run log、语义证据和缓存状态 | 独立测试库、显式 `allow_write_back=true`、模块策略允许、repository 可用且验证计划明确时 | 只经既有 semantic service 和受控 repository；写入 `RecognitionRun` 图谱外日志与图谱内 observation/interpretation；不覆盖来源事实、不提升候选关系 |
| 05 证据融合与缓存 | 当前问题理解首版不做 | 需要 02 检索结果、03 决策和可选 04 识别结果作为输入 | 02/03/04 的输入输出合同稳定后 | 输出 `EvidenceBundle`；保留所有证据来源和 `fact_kind`；冲突不静默覆盖；cache status 可观测；`write_back=false` 下无持久化副作用 |
| 06 答案生成 | 当前问题理解首版不做 | 01 只负责理解问题，不生成最终答案；答案必须基于融合后的证据和 claim | 05 能产出 claim 级可用证据与 answerability 后 | 输出稳定 `AnswerPackage`；每条非诊断 claim 至少有证据引用；candidate、interpretation、formal 使用不同措辞；中文回答不能新增机器答案中不存在的结论 |
| 06 文本模型润色答案 | 首版答案生成不默认做 | 文本模型若不受约束，容易生成未引用事实或改变不确定性 | 确定性 claim 构造和模板回答通过后，仅作为可选渲染器接入 | 文本模型只能改写已批准 claim，不能新增 claim；模型失败时模板回答可完整回退；JSON 仍是权威输出 |
| 07 追溯记录 | 问题理解轻量事件已实现；完整产品审计仍不做 | 完整追溯需要 01-06 的模块事件、source calls、decision、run ids、evidence ids 和 claim ids | 01 已记录规则命中、歧义和澄清原因（`QuestionUnderstandingEvent`）；完整链路在 01-06 串联后实现 | 任一 `AnswerPackage` 可用 `request_id` 回查模块决策、证据、识别运行和 claim；追溯存储故障不得误报为答案失败 |
| 07 用户反馈 | 当前问题理解首版不做写回或提升 | 用户确认不等于来源事实或正式关系；反馈需要审计、权限和候选审核边界 | 完整 `AnswerPackage`、claim id 和 TraceRecord 稳定后 | `confirm/reject/correct/request_review` 有确定状态机；反馈不覆盖来源事实；正式提升只能经 `CandidateReviewService` 和硬规则 |
| `DrawingAssistantService` 端到端编排 | 当前不做完整编排 | 01、03、05、06、07 尚未形成稳定运行时；过早编排会把边界混在一起 | 01 问题理解、02 检索、03 缺口、04 识别、05 融合、06 答案、07 追溯至少有内部合同测试后 | 内部 Python service 串起 01-07；模块只通过显式 DTO 传递状态；不通过全局上下文；默认 `write_back=false`；失败可降级 |
| 产品级 CLI/HTTP/MCP/Web UI adapter | 当前不做 | adapter 不应承载产品业务逻辑；现有 QA CLI/HTTP/MCP 仍是六类固定只读问答兼容入口 | `DrawingAssistantService` 内部 API 稳定后 | adapter 只调用 `DrawingAssistantService`，不直接调用 `GraphRetrievalService`、facade、repository 或 Cypher；HTTP 仍遵守 loopback/认证/脱敏策略；MCP 仍只读优先 |
| 扩展现有 `DrawingGraphQAService` 为自然语言服务 | 不做 | 会破坏 QA 兼容层与产品助手层分工，导致 QAService 职责膨胀 | 原则上不作为主线；只保留 QA 到产品合同的单向 mapper | 现有六类 QA 行为不被破坏；QAService 不反向依赖产品层；新增自然语言能力放入 `QuestionUnderstandingService` |
| Agent/LLM 直接端到端编排工具 | 不作为产品主线 | 难以稳定测试，容易绕过事实分层、facade 白名单、候选/正式边界和追溯合同 | 仅可作为人工辅助或实验原型，不进入权威产品运行时 | 不写 Neo4j、不提升候选、不把模型输出当正式事实；所有正式产品能力仍以 01-07 受控模块为准 |
| Neo4j schema 变更 | 当前不做 | 01 问题理解、02 检索、03 决策、06 答案通常不需要 schema 变更 | 只有 04/05/07 证明既有语义证据、payload、run log 或审计存储无法满足时 | 单独 proposal/design/tasks；迁移脚本可重复执行；live Neo4j 验证单独报告；不把 skipped 集成测试说成通过 |
| 远程 MCP、OAuth、RBAC、TLS、多 worker、HTTP 写回 | 当前不做 | 当前已实现的是本机只读 MCP 和 loopback 只读 HTTP；远程与写回属于部署/安全扩展 | 产品级 service 成熟，且有明确外部使用场景、权限模型和安全评审后 | 认证、授权、审计、限流、脱敏、TLS/部署边界明确；写回能力分工具授权，不能经 HTTP/MCP 绕过应用层 port |

## 3. 按依赖推荐的实施检查点

### 检查点 A：01 问题理解首版（已通过）

已按 Tasks 1-23 完成：

- `QuestionUnderstandingService` 与 `QuestionTextNormalizer`、`ScopeResolver`、`RuleQuestionRouter`、`IntentSplitter`、`EvidenceRequirementFactory`、`ClarificationPolicy`、`QuestionUnderstandingTraceBuilder` 已落地。
- `QuestionUnderstandingModelClient` 协议、fake client 与 `validate_model_output()` 已落地，默认不调用真实模型。
- 与 `GraphRetrievalService` 的只读衔接测试已通过：clarification/unsupported 不触发 facade。
- 专项 122 项、全量 1279 项单元测试通过；4 项 live Neo4j 集成测试跳过，live Neo4j 未验证。

通过标准（均已满足）：

- 明确 ID 的典型问题能稳定生成 `question_type`、scope 和 `required_evidence`。
- 缺少必要 scope 时返回结构化澄清，不触发检索或识别。
- 多意图能拆分为 `AssistantSubrequest`，或在歧义时要求澄清。
- 现有六类 QA 兼容，不修改 `DrawingGraphQAService` 行为。
- 任意问题文本不能把 `allow_write_back` 推断为 true。

### 检查点 B：方案 B 文本模型理解

检查点 A 已通过；协议、fake 与输出校验已落地，真实模型调用仍不做。首个可实施范围应是：

- `QuestionUnderstandingModelClient` 协议
- fake client
- 受约束 JSON 输出合同
- 枚举、scope、证据需求和字段白名单校验
- 模型失败回退为规则结果、澄清或 unsupported

不应在这个检查点做：

- 读取图片
- 调用 Qwen 视觉识别
- 访问 Neo4j
- 生成最终答案
- 写回语义证据或候选关系

### 检查点 C：03 语义缺口判断

进入条件：

- 01 输出的 `EvidenceRequirement` 足够稳定。
- 02 能按这些需求返回 `RetrievalBundle`。
- `fact_kind`、status、freshness、cache key、missing evidence 字段可供判断。

通过标准：

- 能判断 `reuse_existing`、`recognize_required`、`clarification_required`、`unsupported`。
- 有稳定 reason codes。
- candidate 不能满足 formal 需求。
- 禁止识别、预算不足、scope 不完整时不会创建识别目标。

### 检查点 D：04 识别与写回

进入条件：

- 03 已能生成最小识别目标。
- 每个目标都有可信来源事实、image path、bbox、task type、prompt version 和输出合同版本。

通过标准：

- 离线 fake/model 合同测试覆盖请求构造、Schema 校验、错误、超时、脱敏。
- live DashScope 只在明确授权时运行，并记录模型、延迟、token、成本和结构化输出有效性。
- live Neo4j 写回只在独立测试库和显式 `write_back=true` 下运行。
- live DashScope 通过不等于 live Neo4j 写回通过。

### 检查点 E：05-07 与完整产品助手

进入条件：

- 01-04 的内部合同稳定。
- `EvidenceBundle`、`Claim`、`AnswerPackage`、`TraceRecord`、`FeedbackEvent` 字段不再漂移。

通过标准：

- 融合保留冲突和事实等级。
- 答案每条 claim 可追溯。
- 反馈只进入审计或候选审核，不直接覆盖来源事实。
- `DrawingAssistantService` 串联 01-07 后，产品级 adapter 才能新增。

## 4. 新需求评审模板

后续每个产品层新需求分析报告都应补充以下五项：

| 项 | 要回答的问题 |
|---|---|
| 当前是否做 | 本需求是否属于当前检查点，还是只做协议/文档/测试预留 |
| 为什么不做 | 如果暂不做，是因为依赖未稳定、权限未明确、验证不足，还是架构边界不允许 |
| 什么时候可以做 | 需要哪些前置模块、合同、测试、环境或用户授权 |
| 通过标准 | 用哪些单元、合同、离线、live 或写回验证证明完成 |
| 禁止越界 | 明确不能访问哪些层，不能生成哪些事实，不能误报哪些验证状态 |

## 5. 结论

当前最缺的不是更多模块名，而是每个预留能力的实施门槛。01 问题理解首版已按检查点 A 完成并通过专项回归；下一步按 03、04、05、06、07 和 `DrawingAssistantService` 的依赖推进。任何时候都不应为了“快接入”把自然语言理解塞进 `DrawingGraphQAService`，也不应让 adapter、Agent 或自由模型绕过 `DrawingGraphToolFacade`、事实分层、候选审核和默认只读边界；live Neo4j 与 live 模型未验证前不得误报为已验证。
